<UserControl x:Class="SchoolApp.View.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SchoolApp.ViewModel"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:StudentDetailsViewModel />
    </UserControl.DataContext>

    <StackPanel Margin="20">

        <TextBlock Text="First Name"/>
        <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Last Name"/>
        <TextBox Text="{Binding LastName}" />

        <TextBlock Text="Age"/>
        <TextBox Text="{Binding Age}" />

        <TextBlock Text="Class"/>
        <TextBox Text="{Binding Class}" />

        <TextBlock Text="Section"/>
        <TextBox Text="{Binding Section}" />

        <TextBlock Text="Father Name"/>
        <TextBox Text="{Binding FatherName}" />

        <TextBlock Text="Mother Name"/>
        <TextBox Text="{Binding MotherName}" />

        <StackPanel Orientation="Horizontal" Margin="0,10">

            <Button Content="Save"
                    Width="100"
                    Margin="5"
                    Command="{Binding SaveCommand}" />

            <Button Content="Clear"
                    Width="100"
                    Margin="5"
                    Command="{Binding ClearCommand}" />

        </StackPanel>

    </StackPanel>
</UserControl>